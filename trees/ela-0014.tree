\title{Projection}
\author{alex-nelson}
\import{notation}
\date{2025-12-08}
\taxon{definition}
\import{ela}

\p{Let #{V} be a finite-dimensional real vector space. Let #{u,v\in V} be vectors. Assume #{u\neq\vec{0}}. We define the \define{Projection} of #{v} along the #{u} direction to be the vector
##{\Proj{u}{v}=\frac{u\cdot v}{u\cdot u}u=(\widehat{u}\cdot v)\widehat{u}}
where #{\widehat{u}=u/\sqrt{u\cdot u}} is the unit vector in the #{u}-direction.}