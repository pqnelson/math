\title{Product of subsets is nonempty iff subsets are nonempty}
\author{alex-nelson}
\import{notation}
\date{2025-12-16}
\taxon{proposition}
\MML{group_2}{9}{T9}

\p{Let #{G} be a [[gr-000G]]. Let #{A\subset G} and #{B\subset G} be subsets of #{G}. Then #{AB\neq\emptyset} if and only if #{A\neq\emptyset} and #{B\neq\emptyset}.}

\p{(Equivalently, #{AB=\emptyset} if and only if either #{A=\emptyset} or #{B=\emptyset}.)}