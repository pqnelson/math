\title{Sums of submodules are a submodule}
\author{alex-nelson}
\import{notation}
\date{2025-12-18}

\p{Let #{R} be a [[ring-0001]], let #{M} be a module over #{R}, let #{N} and #{K} be [[ra-0031]]s of #{M}. Then #{K+N=\{k+n\in M\mid k\in K,n\in N\}} is a submodule of #{M}.}

\proof{\p{(1) #{K+N} is closed under addition since #{(k+n)+(k'+n')=(k+k')+(n+n')}.}
\p{(2) #{0\in(K+N)} which means #{K+N} is a subgroup of #{M}.}
\p{(3) For any #{r\in R} and #{(k+n)\in K + N} we see #{r(k+n)=(rk)+(rn)} and #{rk\in K} and #{rn\in N} which implies #{(rk)+(rn)\in K+N}.}}