\title{Image of a function}
\author{alex-nelson}
\import{notation}
\date{2025-12-08}
\taxon{definition}

\p{Let #{X} and #{Y} be sets, let #{f\colon X\to Y} be a function. We define the \define{Image} (or \define{Range}) of #{f} to be the subset #{\ran(f)=f(X)=\{f(x)\in Y\mid x\in X\}}.}