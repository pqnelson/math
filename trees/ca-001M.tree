\title{A function is continuous iff it maps convergent sequences to convergent sequences}
\author{alex-nelson}
\import{notation}
\date{2025-12-11}
\taxon{theorem}

\p{Let #{(X,d)} and #{(Y,\rho)} be [[ca-000K]]s. Let #{f\colon X\to Y} be a function. Then #{f} is continuous at #{x} if and only if for every sequence #{(x_{n})} in #{X}, if #{(x_{n})\to x} then #{f(x_{n})\to f(x)} in #{Y}.}

\proof{\p{\forwardproof Assume #{f} is continuous at #{x}. Let #{(x_{n})} be a sequence in #{X} that converges to #{x}. Let #{\varepsilon>0}, we can choose a #{\delta>0} such that if #{d(x,a)<\delta} then #{\rho(f(x),f(a))<\varepsilon}. Since #{(x_{n})\to x} converges, there is an index #{N} such that for all #{n\geq N} we have #{d(x,x_{n})<\delta}. Then #{\rho(f(x),f(x_{n}))<\varepsilon} for all #{n\geq N}. Hence #{f(x_{n})\to f(x)}.}
\p{\backwardproof Assume for any sequence #{(x_{n})} in #{X}, if #{x_{n}\to x}, then #{f(x_{n})\to f(x)} in #{Y}. Assume for contradiction that #{f} is not continuous at #{x}. That is, there is some #{\varepsilon_{0}>0} such that for every #{\delta>0}, there exists an #{a\in B_{\delta}(x)} such that #{\rho(f(x),f(a))\geq\varepsilon_{0}}. In particular, for each #{n}, there exists a point #{x_{n}\in B_{1/n}(x)} such that #{\rho(f(x),f(x_{n}))\geq\varepsilon_{0}}. This defines a sequence #{(x_{n})} in #{X} that converges to #{x} but whose image sequence #{(f(x_{n}))} does not covnerge to #{f(x)}. This contradicts our assumption, hence the result.}}