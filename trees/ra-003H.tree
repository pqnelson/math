\title{Cyclic submodule}
\author{alex-nelson}
\import{notation}
\date{2025-12-18}
\taxon{definition}

\p{Let #{R} be a ring, let #{M} be an #{R}-module, let #{N\subgroup M} be a submodule of #{M} (possibly #{M=N}). We say #{N} is \define{Cyclic} if there exists an #{m\in M} such that
##{N=Rm=\{rm\mid r\in R\}.}}